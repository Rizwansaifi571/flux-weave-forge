import React from "react";
import ReactDOM from "react-dom/client";
import { WallpaperDashboard } from "./WallpaperDashboard";
import "./wallpaper.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <WallpaperDashboard />
  </React.StrictMode>
);
